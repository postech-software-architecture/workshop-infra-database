# Copie para terraform.tfvars (gitignorado) e ajuste.
region  = "us-east-1"
project = "workshop"

# db_password NAO vai aqui: use TF_VAR_db_password vindo de Environment secret.

# Com backend remoto (preferido):
# cluster_state_bucket = "soat-tc3-tfstate"

# Fallback sem backend (ADR-005) — valores do artifact contracts/outputs.json:
# vpc_id_fallback             = "vpc-xxxxxxxx"
# private_subnet_ids_fallback = ["subnet-aaaa", "subnet-bbbb"]
# db_client_sg_id_fallback    = "sg-xxxxxxxx"
